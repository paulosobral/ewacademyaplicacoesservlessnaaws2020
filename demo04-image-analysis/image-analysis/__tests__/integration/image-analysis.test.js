const {
    describe,
    test,
    expect
} = require('@jest/globals')


describe('Image analyser test suite', () => {
    test.todo('it should analyse succesfuly the image returning the results')
    test.todo('given an empty queryString it should return status code 400')
    test.todo('given an invalid ImageURL it should return 500')
})